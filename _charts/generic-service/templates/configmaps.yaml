{{- range .Values.configMaps }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "generic-service.name" $. }}-{{ .name }}
  labels:
    {{- include "generic-service.labels" . | nindent 4 }}
data:
  {{ .subPath | quote }}: {{ .content }} 
{{- end }}
