{{- if .Values.ingress.basicAuth }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "generic-service.name" . }}-basic-auth
  labels:
    {{- include "generic-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  .htpasswd: {{ .Values.ingress.basicAuth }}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: {{ include "generic-service.name" . }}
  labels:
    {{- include "generic-service.labels" . | nindent 4 }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: {{ include "generic-service.name" . }}
  basicAuth:
    users:
      name: {{ include "generic-service.name" . }}-basic-auth
{{- end }}
