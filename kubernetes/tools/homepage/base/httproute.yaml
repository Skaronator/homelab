apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: homepage
spec:
  hostnames:
    - home.wagner.gg
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: private
      namespace: network
  rules:
  - backendRefs:
    - kind: Service
      name: homepage
      port: 3000
