apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmGlobals:
  chartHome: ../../../helm-charts

helmCharts:
  - name: generic-cronjob
    releaseName: lancache-dns-sync
    valuesFile: values.yaml
    namespace: tools
