installCRDs: true

startupapicheck:
  enabled: false

# cannot use kube-system since kustomize overwrites kube-system namespace :/
global:
  leaderElection:
    namespace: "network"

enableCertificateOwnerRef: true
